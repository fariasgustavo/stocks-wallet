import { createUserRepository } from './user.repository.factory';
import { createHobbyRepository } from './hobby.repository.factory';

module.exports = { createUserRepository, createHobbyRepository };
